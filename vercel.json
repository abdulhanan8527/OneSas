{
    "version": 2,
    "builds": [
      {
        "src": "OneSas/wsgi.py",
        "use": "@vercel/python",
        "config": {
          "maxLambdaSize": "15mb",
          "runtime": "python3.9",
          "installCommand": "pip install -r requirements.txt"
        }
      }
    ],
    "routes": [
      {
        "src": "/static/(.*)",
        "dest": "/OneSas/OneSas/static/$1"
      },
      {
        "src": "/(.*)",
        "dest": "OneSas/OneSas/wsgi.py"
      }
    ]
  }